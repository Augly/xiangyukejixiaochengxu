<view class='tap_click'>
  <view class="tap_item {{myindex==index?'item_active':''}}" wx:for="{{tapArr}}" bindtap='click' data-id='{{index}}' wx:key="key">
    {{item.name}}
  </view>
</view>

<view class='Recharge' bindtap='sureRC'>充值</view>

